* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
}

body {
  background-color: #0b0b10;
  color: #fff;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
}

.app-wrapper {
  width: 100%;
  display: flex;
  justify-content: center;
  padding: 10px;
}

.app-container {
  width: 100%;
  max-width: 450px;
  height: 92vh;
  background-color: #161622;
  border-radius: 24px;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  box-shadow: 0 20px 40px rgba(0,0,0,0.8);
  border: 1px solid #28283d;
}

.app-header {
  padding: 14px 18px;
  background-color: #1e1e2e;
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 1px solid #2a2a3e;
}

.app-header h1 {
  font-size: 1.15rem;
  color: #00d2d3;
}

.user-status {
  font-size: 0.75rem;
  background: rgba(46, 213, 115, 0.15);
  color: #2ed573;
  padding: 4px 8px;
  border-radius: 10px;
}

.content {
  flex: 1;
  overflow-y: auto;
  padding: 14px;
}

.hidden {
  display: none !important;
}

.card-box {
  background: #202030;
  padding: 12px;
  border-radius: 14px;
  margin-bottom: 12px;
  border: 1px solid #2a2a3e;
}

.card-box h3 {
  font-size: 0.9rem;
  margin-bottom: 8px;
  color: #00d2d3;
}

.small-text {
  font-size: 0.75rem;
  color: #aaa;
  margin-bottom: 6px;
}

/* Chat */
.chat-box {
  height: 140px;
  background: #181826;
  border-radius: 12px;
  padding: 10px;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-bottom: 8px;
  border: 1px solid #252538;
}

.message {
  max-width: 85%;
  padding: 8px 12px;
  border-radius: 12px;
  font-size: 0.82rem;
  line-height: 1.3;
}

.message.received { background: #2a2a3e; align-self: flex-start; }
.message.sent { background: #546de5; align-self: flex-end; }

.chat-input-zone, .todo-input-zone {
  display: flex;
  gap: 6px;
  margin-bottom: 10px;
}

.chat-input-zone input, .todo-input-zone input {
  flex: 1;
  padding: 8px 12px;
  border-radius: 8px;
  border: 1px solid #2d2d42;
  background: #181826;
  color: white;
  font-size: 0.85rem;
}

/* Jeux */
.game-selector {
  display: flex;
  gap: 6px;
  margin-bottom: 10px;
}

.game-container {
  background: #1a1a28;
  padding: 12px;
  border-radius: 14px;
  text-align: center;
}

.game-status {
  font-size: 0.85rem;
  color: #00d2d3;
  margin-bottom: 8px;
}

/* Morpion */
.tic-tac-toe {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 6px;
  max-width: 180px;
  margin: 0 auto;
}

.cell {
  width: 55px;
  height: 55px;
  background: #252538;
  border: none;
  border-radius: 8px;
  font-size: 1.3rem;
  color: white;
  font-weight: bold;
  cursor: pointer;
}

/* Puissance 4 */
.p4-grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 4px;
  background: #0f0f18;
  padding: 8px;
  border-radius: 10px;
  max-width: 260px;
  margin: 0 auto;
}

.p4-cell {
  width: 30px;
  height: 30px;
  background: #252538;
  border-radius: 50%;
  cursor: pointer;
}
.p4-cell.red { background: #ff4757; }
.p4-cell.yellow { background: #ffa502; }

/* Snake */
#snakeCanvas {
  background: #000;
  border-radius: 10px;
  border: 1px solid #2a2a3e;
}

.touch-controls {
  margin-top: 8px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
}

.touch-controls button {
  padding: 8px 14px;
  border-radius: 6px;
  background: #28283d;
  color: white;
  border: none;
  font-size: 1rem;
}

/* BeReal */
.bereal-banner {
  background: #ff4757;
  color: white;
  padding: 8px 12px;
  border-radius: 10px;
  font-size: 0.8rem;
  margin-bottom: 12px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.friend-checkboxes {
  display: flex;
  gap: 12px;
  font-size: 0.8rem;
  margin-top: 6px;
}

.post-card {
  background: #202030;
  border-radius: 14px;
  padding: 10px;
  margin-bottom: 12px;
}

.post-header {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 8px;
}

.post-media {
  position: relative;
  height: 180px;
  background: #28283d;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.post-media img.captured-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* Permissions Amis & To-Do */
.friend-perm-item {
  background: #181826;
  padding: 8px;
  border-radius: 8px;
  margin-bottom: 6px;
  font-size: 0.8rem;
}

.perm-options {
  display: flex;
  flex-direction: column;
  gap: 2px;
  margin-top: 4px;
  padding-left: 8px;
  font-size: 0.75rem;
  color: #ccc;
}

.todo-list {
  list-style: none;
  font-size: 0.8rem;
}

.todo-list li {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 4px 0;
}

/* Boutons & Nav */
.btn-primary {
  background: #546de5;
  color: white;
  border: none;
  padding: 6px 12px;
  border-radius: 8px;
  cursor: pointer;
  font-size: 0.8rem;
}

.btn-secondary {
  background: #2a2a3e;
  color: white;
  border: none;
  padding: 6px 10px;
  border-radius: 8px;
  cursor: pointer;
  font-size: 0.75rem;
}
.btn-secondary.active { background: #546de5; }

.btn-small {
  background: rgba(255,255,255,0.2);
  color: white;
  border: none;
  padding: 3px 6px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 0.7rem;
}

.bottom-nav {
  display: flex;
  background: #1e1e2e;
  border-top: 1px solid #2a2a3e;
}

.nav-btn {
  flex: 1;
  padding: 12px 4px;
  background: none;
  border: none;
  color: #777;
  font-size: 0.78rem;
  cursor: pointer;
}

.nav-btn.active {
  color: #00d2d3;
  font-weight: bold;
  border-top: 2px solid #00d2d3;
}